export const environment = {
  production: true,
  // PUBLIC_INTERFACE
  /** Base URL for API requests in production. Adjust during deployment if backend lives on another origin. */
  apiBaseUrl: '/api',
};
